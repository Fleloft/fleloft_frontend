query GetPlaceById($ID: ID!) {
  placeById(id: $ID) {
    dbId
    id
    name
    address
    neighborhood
    city
    state
    zipCode
    observations
    rooms{
      id
      number
      status
    }
  }
}
